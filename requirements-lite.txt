# Lite deployment: no ML models (Whisper, sentence-transformers, camel-tools)
# Runs in text-only mode with neutral sentiment fallback
fastapi>=0.104
uvicorn[standard]>=0.24
python-multipart>=0.0.6
numpy>=1.24
scikit-learn>=1.3
